[project]
name = "devicehost-example"
version = "0.1.0"
description = "Example evcc device host exposing a meter with a configurable power value"
requires-python = ">=3.13"
dependencies = [
    "grpcio>=1.83.1",
]

[dependency-groups]
dev = [
    "grpcio-tools>=1.83.1",
]
